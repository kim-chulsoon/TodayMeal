<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <title><%= video ? video.title : "비디오 상세" %></title>
  </head>
  <body>
    <% if (error) { %>
    <h2><%= error %></h2>
    <% } else if (video) { %>
    <h1><%= video.title %></h1>
    <p>채널명: <%= video.channelTitle %></p>
    <p>조회수: <%= video.viewCount %></p>
    <p>좋아요: <%= video.likeCount %></p>
    <iframe
      width="560"
      height="315"
      src="https://www.youtube.com/embed/<%= video.videoId %>"
      title="YouTube video player"
      frameborder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
      allowfullscreen
    >
    </iframe>

    <p><%= video.description %></p>
    <a href="<%= video.videoUrl %>" target="_blank">유튜브에서 보기</a>
    <% } else { %>
    <h2>비디오 정보가 없습니다.</h2>
    <% } %>
  </body>
</html>
